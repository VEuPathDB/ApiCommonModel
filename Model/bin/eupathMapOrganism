#!/usr/bin/perl

use lib "$ENV{GUS_HOME}/lib/perl";
use WDK::Model::CommandHelper;

my $GUS_HOME = $ENV{GUS_HOME};
my $CLASSPATH = &WDK::Model::CommandHelper::getJavaClasspath($GUS_HOME);
my $sysProps = &WDK::Model::CommandHelper::getSystemProps($GUS_HOME, 'eupathMapOrganism');
my $args = &WDK::Model::CommandHelper::getJavaArgs(@ARGV);
my $envJvmOpts = $ENV{GUSJVMOPTS};

my $cmd = "java $sysProps $envJvmOpts -classpath $CLASSPATH org.gusdb.wdk.model.fix.OrganismUpdater $args";

$status = system($cmd);

exit $status >> 8;
