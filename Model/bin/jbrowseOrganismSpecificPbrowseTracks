#!/usr/bin/perl

use strict;
use lib $ENV{GUS_HOME} . "/lib/perl";
use JSON;
use ApiCommonModel::Model::JbrowseOrgSpecificAaTracks;

my ($orgAbbrev, $projectName, $buildNumber, $webservicesDir, $applicationType) = @ARGV;

my $result = {"tracks" => [] };

ApiCommonModel::Model::JbrowseOrgSpecificAaTracks::processOrganism($orgAbbrev, $projectName, $buildNumber, $webservicesDir, $applicationType, $result);

print encode_json($result);
