#!/usr/bin/perl

use strict;
use lib $ENV{GUS_HOME} . "/lib/perl";
use JSON;
use ApiCommonModel::Model::JbrowseOrgSpecificNaTracks;

my ($orgAbbrev, $projectName, $isApollo, $buildNumber, $webservicesDir, $applicationType) = @ARGV;

my $result = {"tracks" => [] };

ApiCommonModel::Model::JbrowseOrgSpecificNaTracks::processOrganism($orgAbbrev, $projectName, $isApollo, $buildNumber, $webservicesDir, $applicationType, $result);

print encode_json($result);

