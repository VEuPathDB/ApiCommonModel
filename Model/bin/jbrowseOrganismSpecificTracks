#!/usr/bin/perl

use strict;
use lib $ENV{GUS_HOME} . "/lib/perl";
use JSON;
use ApiCommonModel::Model::JbrowseOrganismSpecificNaTracks;

my ($orgAbbrev, $projectName, $isApollo, $buildNumber, $webservicesDir, $applicationType) = @ARGV;

my $result = {"tracks" => [] };

ApiCommonModel::Model::JbrowseOrganismSpecificNaTracks::processOrganism($organismAbbrev, $projectName, $buildNumber, $webServicesDir, $applicationType, $result);

print encode_json($result);

