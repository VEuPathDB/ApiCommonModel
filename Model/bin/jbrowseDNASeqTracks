#!/usr/bin/perl

use strict;
use lib $ENV{GUS_HOME} . "/lib/perl";
use JSON;
use ApiCommonModel::Model::JbrowseDnaSeqTracks;

my ($organismAbbrev, $projectName, $buildNumber, $webServicesDir, $applicationType) = @ARGV;

my $result = {"tracks" => [] };

ApiCommonModel::Model::JbrowseDnaSeqTracks::processOrganism($organismAbbrev, $projectName, $buildNumber, $webServicesDir, $applicationType, $result);

print encode_json($result);


1;

