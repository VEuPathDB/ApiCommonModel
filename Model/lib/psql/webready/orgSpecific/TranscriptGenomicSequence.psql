drop table if exists :SCHEMA.:CLEAN_ORG_ABBREVTranscriptGenomicSequenceTmp;

/*
ATTENTION:  This script is used to make an empty table.  It will be populated with a perl script.

We make the table here in the usual way so we get partitioned webready table
*/


create unlogged table :SCHEMA.:CLEAN_ORG_ABBREVTranscriptGenomicSequenceTmp (
       org_abbrev              varchar(30),
       gene_source_id          varchar(80),
       source_id               varchar(80),
       project_id              varchar(80),
       genomic_sequence        text,
       genomic_sequence_length integer,
       modification_date       timestamp
  );


:CREATE_AND_POPULATE
select * from :SCHEMA.:CLEAN_ORG_ABBREVTranscriptGenomicSequenceTmp;
:DECLARE_PARTITION;

drop table :SCHEMA.:CLEAN_ORG_ABBREVTranscriptGenomicSequenceTmp
;
