<wdkModel>

  <querySet name="StrainSegmentId" queryType="id" isCacheable="true">

    <!-- MULTI STRAIN.  The strain param is multiPick, so this query returns ONE row per
         selected strain: the sequence side (one row) is crossed with the unnested strain
         list and the gates below are applied to each pair.  Nothing downstream needed to
         change for that, since it was already written against a set of primary keys.

         Because the gates are per pair, a strain that fails one is simply absent from the
         result while its companions still return: asking for two valid strains and one
         nonsense strain yields two rows, not an error.

         FIVE gates, all as filters so bad input returns zero rows.  The numbering
         below is the SAME as spec section 5.1, so "gate 3" here and "gate 3" there
         are the same gate; do not renumber one without the other:
           1. source_id  - the reference sequence exists (dots.ExternalNaSequence.source_id)
           2. range      - 1 <= start <= end <= ens.length (end=0 means "to end of
                           sequence" and is normalized to ens.length in the
                           derived table, before this gate ever sees it)
           3. EXISTS     - the strain has indel data somewhere on this sequence's
                           ORGANISM, matched on taxon_id
           4. colon      - seg.ref_seq contains no ':', the only delimiter in the
                           minted primary key (see the gate's own comment below)
           5. organism   - the sequence's taxon_id has a matching apidb.organism row
                           (the inner join, not just a project_id source; see below)

         Gate 3 exists to prove strain and sequence are mutually consistent, which
         is why this query needs no separate organism input to validate against.
         It is scoped by taxon_id, never by strain name alone.  On unidb_shu_a (the
         live appDb, measured 2026-07-31) that scope currently costs nothing to
         guarantee: 452 '_Indel' protocol app nodes carry 452 distinct names and 0
         names span more than one organism.  On the much larger genomicsdb_rebuild01
         (6,119 strain names) 126 names resolved to two or more nodes; the taxon
         scope is what keeps this query correct whenever that situation recurs here.
         The full evidence for organism-level scoping (including the three-organism
         FASTA coverage check) lives in spec section 5.1.1 and is deliberately NOT
         duplicated here, so that only one copy of the numbers can go stale.

         Gate 5, the apidb.organism join, is a gate and not just a project_id source:
         it is an inner join, so a sequence whose taxon_id has no apidb.organism row
         yields no record at all. That is intended - such a sequence sits outside
         every project and so has no project_id to key a record on - and it does
         not need to be a LEFT JOIN, since a NULL project_id would produce a
         malformed primary key.

         project_id comes from apidb.organism.project_name because it is
         unpartitioned and correct on every project, avoiding the per-project
         query duplication that forces DynSpan to maintain UniDB-specific
         variants. -->
    <sqlQuery name="StrainSegmentsByRefSegment" doNotTest="true">
      <paramRef ref="StrainSegmentParams.strain" quote="true"/>
      <paramRef ref="sharedParams.sequenceId"/>
      <paramRef ref="sharedParams.start_point" default="1"/>
      <paramRef ref="sharedParams.end_point_segment"/>
      <paramRef ref="SpanParams.sequence_strand"/>

      <column name="source_id"/>
      <column name="project_id"/>

      <sql>
        <![CDATA[
          -- $$strain$$ is a multiPick flatVocab with quote="true", so WDK renders it as a
          -- comma separated list of quoted values ('a','b','c'): EnumParamHandler quotes
          -- each internal value and joins them with DBPlatform.prepareExpressionList,
          -- which on PostgreSQL is a plain comma join.  That is precisely an ARRAY[...]
          -- element list, so unnesting it gives one row per selected strain, and the
          -- sequence side is crossed with it to mint one primary key per strain.  Every
          -- gate below is then evaluated once per strain, unchanged in meaning.
          SELECT CONCAT(st.strain, ':', seg.ref_seq, ':',
                        seg.ref_start, '-', seg.ref_end, ':',
                        $$sequence_strand$$)              AS source_id
               , seg.project_id
          FROM (
            SELECT ens.source_id                          AS ref_seq
                 , o.project_name                         AS project_id
                 , $$start_point$$::integer               AS ref_start
                 , CASE WHEN $$end_point_segment$$::integer = 0
                          THEN ens.length::integer
                        ELSE $$end_point_segment$$::integer
                   END                                     AS ref_end
                 , ens.length::integer                     AS seq_length
                 , ens.taxon_id
            FROM dots.ExternalNaSequence ens
               , apidb.organism o
            WHERE o.taxon_id = ens.taxon_id
              AND ens.source_id = $$sequenceId$$
          ) seg
             -- One row per selected strain.  ARRAY[...] is a constant expression here, so
             -- no LATERAL is needed; the planner turns the whole thing into a Nested Loop
             -- Semi Join driven by the (single row) sequence side.
             , unnest(ARRAY[$$strain$$]) AS st(strain)
          WHERE seg.ref_start >= 1
            AND seg.ref_end   >= seg.ref_start
            AND seg.ref_end   <= seg.seq_length
            -- GATE 4 (spec 5.1 numbering).  ':' is the ONLY delimiter in the primary
            -- key this query mints, so a reference sequence whose source_id contains
            -- one would produce an unparseable ID -- StrainSegmentAttributes.Coords
            -- would split it at the embedded colon and then abort the ENTIRE attribute
            -- query (every row on the page, not just the offender) casting
            -- 'ncRNA'::integer.
            --
            -- What this gate guarantees is SEQUENCE-SIDE ONLY.  It is not a
            -- by-construction property of the whole ID: st.strain is concatenated in
            -- ahead of the first colon and is never colon-checked here.  What keeps
            -- the strain field safe is the flatVocab it comes from, all of whose
            -- values are colon-free today (0 of 452 on unidb_shu_a) -- a property of
            -- the data, not of this SQL.
            --
            -- Neither side is an enforced invariant, which is exactly why the gate is
            -- worth its keystrokes.  Provenance of the numbers, because two databases
            -- get confused here:
            --   unidb_shu_a (the LIVE appDb, measured 2026-07-31): 0 of 160,581
            --     dots.ExternalNaSequence source_ids contain ':'; 120 sequences carry
            --     indel rows (1,855,449 rows over 1,356 protocolappnode rows).
            --   genomicsdb_rebuild01 (a DIFFERENT, larger database): 10,704
            --     colon-bearing source_ids, e.g. bld68_Tb927.1.05:mRNA, a
            --     transcript-level feature; 20,823 indel-bearing sequences, none of
            --     them colon-bearing.
            -- So on the live DB the gate is currently unreachable, and on rebuild01 it
            -- was unreachable only by an accident of which features get indel-called.
            AND seg.ref_seq NOT LIKE '%:%'
            -- The suffix is appended to the unnested strain (CONCAT(st.strain, '_Indel'))
            -- rather than stripped from the column (regexp_replace(pan.name, ...)),
            -- so pan_named_ix (name, protocol_app_node_id, ...) can still be used as
            -- an Index Only Scan; applying a function to pan.name instead would force
            -- a scan of protocolappnode per row.  This is why the multiPick change
            -- unnests the strain list rather than turning the gate into
            -- regexp_replace(pan.name, '_Indel$', '') IN ($$strain$$): keeping pan.name
            -- on the bare side of an equality is what preserves the Index Only Scan, and
            -- it stays an equality once per strain (verified below: "Index Searches: 5"
            -- for five strains).
            --
            -- GATE 3 (spec 5.1 numbering).
            -- The gate is on ORGANISM (ens2.taxon_id = seg.taxon_id), not on the exact
            -- sequence (i.na_sequence_id = seg.na_sequence_id).  What it proves is the
            -- only thing this query needs: THIS STRAIN WAS SEQUENCED AGAINST THIS
            -- ORGANISM, so the strain name and the reference sequence are mutually
            -- consistent and the minted primary key is meaningful.  What it
            -- deliberately no longer proves is that the strain has indel data on this
            -- exact contig -- because that is not a precondition for a valid request.
            -- A strain with ZERO indels on a contig is the ordinary case of a contig
            -- that simply matches the reference: the strain coordinates equal the
            -- reference coordinates, the identity mapping is exactly what
            -- StrainSegmentAttributes.Coords already returns via its LEFT JOIN +
            -- COALESCE(..., 0), and the contig IS present in that strain's consensus
            -- FASTA.  Gating on the exact sequence refused 689 of the 6,068 valid
            -- strain/sequence pairs on unidb_shu_a (11%), e.g. A17-10A-1 +
            -- mito_A_fumigatus_Af293, whose defline >A17-10A-1_mito_A_fumigatus_Af293
            -- is right there in A17-10A-1_consensus.fa.gz.
            --
            -- Organism-level scoping is safe to substitute because, on unidb_shu_a
            -- (measured 2026-07-31), (strain name, taxon) resolves to exactly one
            -- protocol_app_node in all 452 pairs present, and no protocol_app_node_id
            -- spans more than one organism.  The residual assumption, and the FASTA
            -- coverage evidence for it across three organisms, are in spec 5.1.1 and
            -- spec 10; in one line, this gate is sound only while a strain set covers
            -- every contig of its organism's reference.
            --
            -- Cost, both paths, measured on unidb_shu_a 2026-07-31 (EXPLAIN ANALYZE):
            --   ACCEPT ~0.33 ms for one strain.  The EXISTS early-exits on the first
            --     matching indel row via indel_ix1.
            --   REJECT is where this gate costs something, and it is unavoidable:
            --     a false EXISTS must exhaust every indel row the strain owns.  For
            --     the largest strain here, A17-48H-7 (108,091 indel rows), a
            --     cross-organism reject runs ~101 ms versus ~53 ms for the old
            --     exact-sequence gate, i.e. about 2x.  It scales with the strain's
            --     row count, not the DB's: the smallest strain, C044 (4 rows),
            --     rejects in ~0.33 ms.  On a 43.5M-row database the worst case would
            --     be substantially larger.
            --
            -- Cost of the multiPick change itself, same DB and date, Chr1 396000-399000:
            --   ACCEPT  1 strain   est cost 238.24, ~0.35 ms
            --   ACCEPT  5 strains  est cost 939.65, ~0.61 ms  (5 rows)
            --   ACCEPT  all 452    ~1.9 s (232 rows; 220 strains belong to other organisms
            --                      and are gated out)
            --   REJECT  1 strain   ~4.9 ms   |  REJECT  5 strains  ~22.5 ms
            -- So wall time on the accept path is roughly flat in strain count while the
            -- reject path is linear in it, which is the same asymmetry the single-strain
            -- query already had, just multiplied.  The plan stays a Nested Loop Semi Join
            -- with an Index Only Scan on pan_named_ix, one index search per strain; the
            -- unnest is a Function Scan costing 0.05.  Five strains in one search is
            -- cheaper than five separate searches, which would repeat the sequence lookup
            -- and the WDK query-instance overhead five times.  The unbounded worst case is
            -- capped by maxSelectedCount on the param (see strainSegmentParams.xml).
            --   Do NOT "optimise" this into a LIMIT 1 scalar subquery; that rewrite
            --     was considered and rejected on purpose (spec 5.1.1) because it
            --     would silently return a wrong answer if the "no node spans >1
            --     organism" invariant ever broke, whereas EXISTS degrades to slow.
            AND EXISTS (
                  SELECT 1
                  FROM apidb.indel i
                     , study.protocolappnode pan
                     , dots.ExternalNaSequence ens2
                  WHERE i.protocol_app_node_id = pan.protocol_app_node_id
                    AND pan.name = CONCAT(st.strain, '_Indel')
                    AND ens2.na_sequence_id = i.na_sequence_id
                    AND ens2.taxon_id = seg.taxon_id
                )
        ]]>
      </sql>
    </sqlQuery>

  </querySet>

</wdkModel>
